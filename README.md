# Scroll Trigger With Pin
    - Use Pin on parent
    - Here I was triggering an event on heading. But I wanted it to be executed only after the entire heading appears after scroll.
    - Therefore use Pin on parent. Here -> trigger is "page1"